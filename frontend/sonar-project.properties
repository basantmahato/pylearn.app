# Disable SonarLint rules that conflict with intentional inline-style patterns
sonar.issue.ignore.multicriteria=e1,e2

# Inline CSS styles — intentional for dynamic theming
e1.ruleKey=typescript:S6850
e1.resourceKey=**/*.tsx

# Duplicate IDs via dynamic expressions — false positive in .map()
e2.ruleKey=Web:S5255
e2.resourceKey=**/*.tsx
